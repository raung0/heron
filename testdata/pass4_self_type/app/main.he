package main

Node :: struct {
	value: int

	pub clone :: fn (self: &Self) -> Self do Self .{ .value = self.value }
	pub with_value :: fn (value: int) -> Self do Self .{ .value }
}

main := fn {
	value := Node .{ .value = 5 }
	_ = value.clone()
}
