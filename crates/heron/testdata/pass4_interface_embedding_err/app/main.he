package main

Reader :: interface {
	pub read := fn [[runtimeable]] (self: &Self) -> int ---
}

ReadWriter :: interface extends Reader {
	pub write := fn [[runtimeable]] (self: &Self; x: int) ---
}

main := fn {
	_ = 0
}
